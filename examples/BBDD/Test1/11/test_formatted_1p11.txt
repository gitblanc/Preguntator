?: Dado el esquema relacional R = { A, B, C, D, E }, en una relación r(R) cualquiera
a. Nunca puede existir una clave externa
b. { A, B } es una superclave
c. { A } nunca podría ser clave primaria
$d. { A, B, C, D, E } podría ser una clave primaria
e. Ninguna de las otras es correcta

?: Dado el siguiente diagrama Entidad-Relación en los mecanismos normales de representación mediante tablas son representaciones válidas
[image2.png]
$a. A(_a1_,a2,a3) R(_a1-papel1, a1-papel2_, b1, x) B(_b1_, b2)
b. A(_a1_,a2,a3,x) R(_a1-papel1_, a1-papel2, b1) B(_b1_, b2)
c. A(_a1_,a2,a3,x) R(_a1-papel1, a1-papel2_, b1) B(_b1_, b2)
d. A(_a1_,a2,a3) R(_a1-papel1_, a1-papel2, _b1_, x) B(_b1_, b2, x)
e. Ninguna de las otras es correcta

?: En los mecanismos normales de representación mediante tablas de un conjunto de relaciones BINARIO normal del modelo Entidad-Relación
a. Podría no aparecer ninguna clave externa
b. Los atributos del conjunto de relaciones siempre forman parte de la clave de la tabla, si se genera una
c. Siempre debe generarse una tabla específicamente
d. En las tablas generadas obligatoriamente aparece más de una clave candidato
$e. Ninguna de las otras es correcta

?: Dado el siguiente diagrama Entidad-Relación en los mecanismos normales de representación mediante tablas son representaciones válidas
[image4.png]
a. B(K, a1, a2, b1, b2) C(K, c1, c2)
b. B(K, b1, b2, c1, c2)
c. B(K, a1, a2, b1, b2) C(K, a1, a2, c1, c2)
d. A(K, a1, a2, c1, c2) B(b1, b2)
$e. Ninguna de las otras es correcta

?: La restricción de exclusión dentro del modelo E-R
a. Se establece entre varios C. Relaciones denominados "fuertes" y varios C. Relaciones "débiles"
b. Hace referencia a que las entidades de "alto nivel" en una generalización no pueden tener relaciones con las de "bajo nivel"
$c. Puede usarse a la vez que la restricción de cardinalidad máxima
d. Se considera parte del nivel simple del modelo E-R
e. Ninguna de las otras es correcta

?: Dado el diagrama Entidad-Relación que muestra una agregación de las entidades A y B (con claves a y b, y atributos a1 y b1, respectivamente), asociadas por una relación R de muchos a muchos entre A y B, en principio son representaciones válidas del diagrama dentro del modelo relacional
a. A(_a_,a1)    B(_b_,b1)    R(_a,b_)  Agr(_a,b_)
$b. A(_a_,a1)    B(_b_,b1)    Agr(_a,b_)
c. A(_a_,a1)    R(_a,b_,b1)    Agr(_a,b_)
d. A(_a_,a1)    B(_b_,b1,a)    R(_a,b_)
e. Ninguna de las otras es correcta

?: La operación de producto natural del álgebra relacional
a. Es una operación fundamental del álgebra relacional
$b. Puede sustituirse siempre por una combinación de las operaciones de producto cartesiano, selección y proyección
c. Cumple la misma función que el cuantificador universal del cálculo relacional
d. Puede devolver más tuplas que el producto cartesiano
e. Ninguna de las otras es correcta

?: Dado un conjunto de entidades Persona, con atributos "DNI" (clave primaria), "nombre", y un atributo multivaluado "afición", una representación válida en el modelo relacional puede ser:
a. Persona(_DNI_, nombre, afición)
$b. Persona(_DNI_, nombre)   PA(_afición, DNI_)
c. Persona(_DNI, afición_, nombre)
d. Persona(_DNI_, nombre)   PA(_afición_, DNI)
e. Ninguna de las otras es correcta

?: El ciclo de vida de las bases de datos que usamos
a. Está más orientado al diseño dirigido por procesos
b. Es una metodología completa
c. No se usa en la práctica al generar esquemas muy ineficientes
d. Está orientado al modelo orientado a objetos
$e. Ninguna de las otras es correcta

?: El componente gestor de transacciones de un SGBD
a. Almacena los metadatos
$b. Gestiona el control de concurrencia en el acceso a los datos de la BD
c. Convierte las definiciones expresadas con el DDL a metadatos, que almacena otro componente
d. Se ocupa de controlar los periféricos de E/S, como discos duros
e. Ninguna de las otras es correcta